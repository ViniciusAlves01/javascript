let vetor = [1,5,3,7] //Declaração do array.

console.log(`Antes da organização: ${vetor}`)
console.log(`Depois da organização ${vetor.sort()}\n`) //Usar com cuidado, pois a organização é feita através da casa decimal.

console.log(`Comprimento: ${vetor.length}\n`) //Para saber o comprimento do array com o atributo length.